import React from 'react';

import Routing from './Routing';

import 'assets/styles/resets.scss';

export const App = () => {
    return (
        <div className="App">
            <Routing />
        </div>
    );
};
